Template.footer.events({
    "submit form": function (event, template) {
        event.preventDefault();

        console.debug("Send");
        // TODO: App parse

        // TODO: Email.send(), assets
    }
});
